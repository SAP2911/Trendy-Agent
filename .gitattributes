# Normalize line endings: LF in the repo, native in the working tree.
* text=auto eol=lf

# The fixed dataset and policy are evaluated verbatim — never touch them.
orders.json        -text
trendly_policy.md  -text

*.pdf   binary
*.png   binary
*.ico   binary
*.webp  binary
